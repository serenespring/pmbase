function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

import * as React from 'react';
export var ChipManger = /*#__PURE__*/function () {
  function ChipManger(chips) {
    var _this = this;

    _classCallCheck(this, ChipManger);

    this.chips = void 0;
    this.chipsCache = new Map();
    this.frame = 0;
    this.chips = chips;
    (this.chips || []).forEach(function (chip) {
      _this.chipsCache.set(chip.path.join('___'), chip);
    });
  }

  _createClass(ChipManger, [{
    key: "getChip",
    value: function getChip(path) {
      return path && this.chipsCache.get(path.join('___'));
    }
  }, {
    key: "setFrame",
    value: function setFrame(frame) {
      this.frame = frame;
    }
  }]);

  return ChipManger;
}();
var SVGContext = /*#__PURE__*/React.createContext({
  chipManger: new ChipManger()
});
export default SVGContext;