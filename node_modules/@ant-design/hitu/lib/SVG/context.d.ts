import * as React from 'react';
import { Chip } from '../interface';
export declare class ChipManger {
    private chips?;
    private chipsCache;
    private frame;
    constructor(chips?: Chip[]);
    getChip(path?: number[]): Chip;
    setFrame(frame: number): void;
}
export interface SVGContextProps {
    chipManger: ChipManger;
    theme?: Record<string, string>;
}
declare const SVGContext: React.Context<SVGContextProps>;
export default SVGContext;
