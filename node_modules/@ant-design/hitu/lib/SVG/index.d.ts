import * as React from 'react';
export interface SVGProps extends Omit<React.SVGAttributes<any>, 'path'> {
    tagName: string;
    path?: number[];
}
declare function SVG({ tagName: Component, path, ...rest }: SVGProps): React.ReactElement | null;
declare namespace SVG {
    var parse: (text: string, { debug, name, width, height }?: ParseOption) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
}
export interface ParseOption {
    debug?: boolean;
    name?: string;
    width?: number;
    height?: number;
}
export default SVG;
export declare function getColors(text: string): RegExpMatchArray;
